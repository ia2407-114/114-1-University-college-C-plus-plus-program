int main(void)
{
	int p, r, q, w, x, y, z;

	printf("Enter p\n"); // promt
	scanf_s("%d", &p);
	printf("Enter r\n"); // promt
	scanf_s("%d", &r);
	printf("Enter q\n"); // promt
	scanf_s("%d", &q);
	printf("Enter w\n"); // promt
	scanf_s("%d", &w);
	printf("Enter x\n"); // promt
	scanf_s("%d", &x);
	printf("Enter y\n"); // promt
	scanf_s("%d", &y);

	z = p * r % q + w / x - y ; // Operation to z

	printf("%d * %d %% %d + %d / %d - %d and the z is %d\n",p, r, q, w, x, y, z);
	return 1;
}
