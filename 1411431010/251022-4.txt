#include <iostream>
int main()
{
	double pi = 0.0;
	double s = 1.0;
	int item;
	printf("請輸入項數:");
	scanf_s("%d", &item);
	for (int i = 0; i < item; i++)
	{
		pi += s * 4.0 / (2 * i + 1);
		s *= -1.0;
		
	}
	printf("近似pi的值=%.6f\n", pi);
	scanf_s("%f", &pi);

	return 0;
}