#include <stdio.h>

int main(void)
{
unsigned int i, term;

float result = 1.0, fact = 1.0;

scanf_s("%d", &term);

for (i = 1;i <= term;i++)
{
fact *= i;

result += (float)1 / fact;
}

printf_s("%d的e結果為:%.5f\n", term, result);
}