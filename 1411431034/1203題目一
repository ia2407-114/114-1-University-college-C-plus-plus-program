#include <stdio.h>

double fac(int);

int main(void) {
	int n;
	double e = 1;
	printf("輸入項目數量");
	scanf_s("%d", &n);
	for (int i = 1; i <= n; i++)
		e = e+ (1 / fac(i));
	printf("%lf", e);
}
double fac(int n) {
	if (n <= 1)
		return 1;
	else
		return n * fac(n - 1);
}
