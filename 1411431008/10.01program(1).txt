#include <stdio.h>

int main(void) {
    int p, r, q, w, x, y, z;

    printf("請輸入 6 個要計算的整數：\n");

    scanf_s("%d %d %d %d %d %d", &p, &r, &q, &w, &x, &y);

    z = p * r % q + w / x - y;

    printf("結果是: %d = %d * %d \% %d + %d / %d - %d\n", z, p, r, q, w, x, y);

    return 0;
}