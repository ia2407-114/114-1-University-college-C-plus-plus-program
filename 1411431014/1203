#include<stdio.h>

int fac(int n) {
	if (n <= 1)
		return 1;
	else
		return n * fac(n - 1);
}

int main() {
	int n;
	double e = 0;
	printf("n=");
	scanf_s("%d", &n);
	for (int i = 0; i <= n; i++) {
		e += 1.0 / fac(i);
	}

	printf("e 的近似值為:%.15lf\n", e);

	return 0;


}
