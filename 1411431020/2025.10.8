#include<stdio.h>

int main(void)
{
    int a, b, c;
    printf("Enter three different integers:\n");
    scanf_s("%d %d %d", &a, &b, &c);
    printf("Sum is %d\n", a+b+c);
    printf("Average is %f\n",((float)a+b+c)/3);
    printf("The Product is %d\n",a*b*c);
    if (a > b && a > c) {
            printf("Largest is %d\n", a );
    }
    if (c > b && c > a) {
            printf("Largest is %d\n", c);
    }
    if(b > a && b > c) {
            printf("Largest is %d\n", b);
    }
    if (a < b && a < c) {
            printf("smalles is %d\n", a);
    }
    if (c < b && c < a) {
            printf("smalles is %d\n", c);
    }
    if (b < a && b < c) {
            printf("smalles is %d\n", b);
    }
        




    return 0;
}








#include<stdio.h>

int main(void)
{
	int BMI, weight_In_Kilograms, height_In_Meters;
	printf("Enter your weight in Kilograms and height in Meters :\n");
	scanf_s("%d", &weight_In_Kilograms);
    scanf_s("%f", &height_In_Meters);
	BMI=weight_In_Kilograms / height_In_Meters / height_In_Meters
	printf("BMI is %f\n", (float)BMI);
    if (BMI < 18.5) {
            printf("Your BMI is Underweight\n");
    }
     eles if(BMI < 24.9 && BMI >= 18.5) {
            printf("Your BMI is Underweight\n");
    }
     eles if (BMI < 25 && BMI >= 18.5) {
            printf("Your BMI is Underweight\n");
    }
    if (BMI > 30) {
            printf("Your BMI is Underweight\n");
    }


	return 1;
}
