#include <stdio.h>


float pi_1(int y) {
	float pi = 4.0;
	int i;

	for (i = 1;i < y;i++) {
		if (i % 2 == 0)
			pi = pi + 4.0 / (i * 2 + 1);
		else if (i % 2 != 0)
			pi = pi - 4.0 / (i * 2 + 1);

	}
	return pi;
}
