#include <stdio.h>
int main(void) {
	int term, i, fact=1;
	double e = 1.0;
	printf("請輸入項目數量 term < 13: ");
	scanf_s("%d", &term);
	for (i = 1; i < term; i++) {
		fact *= i;
		
	}
	printf(" 階乘為: %d\n", fact);
	return 0;
}
